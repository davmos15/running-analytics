[pytest]
pythonpath = .
testpaths = tests/python
# logfire (pulled in transitively) registers a pytest plugin with a broken
# import; we don't use it, so disable its autoload.
addopts = -p no:logfire
